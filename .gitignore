# dependencies and build output
/node_modules
/.npm-cache
/.next
/out

# local secrets
.env
.env.*
!.env.example

# tooling
*.tsbuildinfo
npm-debug.log*
.DS_Store

# runtime-writable server data (e.g. uploaded PID master overrides) — not shipped, not versioned
/data

# source archives are preserved locally, not shipped with the app
*.zip

nexs proxy.md